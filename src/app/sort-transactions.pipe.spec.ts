import { SortTransactionsPipe } from './sort-transactions.pipe';

describe('SortTransactionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTransactionsPipe();
    expect(pipe).toBeTruthy();
  });
});
